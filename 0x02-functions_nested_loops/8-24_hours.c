#include <stdio.h>
#include<stdlib.h>
#include <math.h>
#include <time.h>
int main()
{
int T,t,Time;
while (T <= 23 && t <= 59)
{
for(T >= 0;T++;);
for(t <= 0;t++;);
putchar('T');
putchar('T');
putchar(':');
putchar('t');
putchar('t');
putchar('\n');
}
return(0);
}
