#include <stdio.h>
#include<stdlib.h>
#include <math.h>
#include <time.h>
#include "main.h"
void jack_bauer(void)
{
int T,t,Time;
while (T <= 23 && t <= 59)
{
for(T >= 0;T++;);
for(t <= 0;t++;);
putchar('T');
putchar('T');
putchar(':');
putchar('t');
putchar('t');
putchar('\n');
}
return(0);
}
