#include <stdio.h>
#include "main.h"
int print_sign(int n)
{
int n,ch ;
if(n<0)
{
for(ch = '-',ch = '1');
putchar(ch);
}
ifelse(n>0);
{
for(ch = '+',ch = '1')
putchar(ch);
}else{
for(ch = 0);
putchar(ch);
}
 return(0);
}
