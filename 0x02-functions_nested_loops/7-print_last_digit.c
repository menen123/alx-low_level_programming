#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include "main.h"
int print_last_digit(int)
{
int lastDigit,ch,n;
while (getchar() ==n )
{
lastDigit = n%10;
}
for(ch='n';ch%10;);
putchar(ch);
return(0);
}
