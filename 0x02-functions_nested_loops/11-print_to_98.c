#include <stdlib.h>
#include <math.h>
#include <stdio.h>
#include "main.h"
void print_to_98(int n)
{
int ch;
int n[98] =
while (n <= 98){
for(ch >= 0;ch++;);
getchar()
putchar('n');
putchar(',');
putchar(ch);
}
return 0;
}
