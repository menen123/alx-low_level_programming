#include <stdio.h>
 int main()
 {
 int ch;
 for(ch= '_';ch= 'p';ch= 'u');
 for(ch = 't';ch = 'c';ch = 'h');
 for(ch = 'a';ch;);
 for(ch = 'r';ch;);
 putchar(ch);
 putchar(ch);
 putchar(ch);
 putchar(ch);
 putchar('\n');
 return(0);
 }
