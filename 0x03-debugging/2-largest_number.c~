#include <stdlib.h>
#include <stdio.h>
#include "main.h"
int main()
{
if (a > b &&  b>c)
{
printf("largest = %d",a);
 scanf("%d",&a)
}
else if (b>a && a>c)
{
printf("largest = %d",b);
}
else
{
printf("largest=%d",c);
{
return(0);
}
