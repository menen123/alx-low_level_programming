#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#pragma once
{
void char "positive_or_negative(i)";  
int i;
void positive_or_negative(int i);
if (i > 0)
return positive_or_negative(i) = i is positive
else (i < 0)
return positive_or_negative(i) = i is negative
}
int a;
int b;
int c; 
#endif

